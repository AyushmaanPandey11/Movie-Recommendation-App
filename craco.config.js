// craco.config.js

module.exports = {
    webpack: {
      configure: (webpackConfig, { env, paths }) => {
        // Modify webpackConfig as needed
        return webpackConfig;
      },
    },
  };
  